<!DOCTYPE html>
<html>
  <head>
    <title>MtG</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "styles", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag t('http_cdn_jsdelivr_net_npm_keyrune_latest_css_keyru') %>

  </head>

  <body class="container-fluid">
    <div class="row bg-primary">
      <h1 class="col-6 mt-3 text-light">MtG Cardbase </h1>
      <div class="col-4 d-flex align-items-center justify-content-center">
        <a href="<%= root_path %>" class="text-light h5 me-3 btn btn-outline-secondary">Cards</a>
        <a href="<%= decks_path %>" class="text-light h5 btn btn-outline-secondary">Decks</a>
      </div>
      <div class="col-2 d-flex align-items-center justify-content-center">
        <% if user_authenticated %>
          <a href="<%= logout_path %>" class="text-light h5 btn btn-outline-secondary">Sign Out</a>
        <% else %>
          <a href="<%= login_path %>" class="text-light h5 me-3 btn btn-outline-secondary">Log In</a>
          <a href="<%= signup_path %>" class="text-light h5 btn btn-outline-secondary">Sign Up</a>
        <% end %>
      </div>
    </div>
    <div class="row">
      <%= yield %>
    </div>
  </body>
</html>
